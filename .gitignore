# Dependencies — installed by `npm ci`, never tracked.
node_modules/

# Astro build output — produced by the GitHub Pages workflow, never tracked.
dist/

# Astro dev cache
.astro/

# Environment variables — must never be committed.
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS noise
.DS_Store